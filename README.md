# daemonset-simple-task

DaemonSet plugin

This daemon set plugin prints messages to an `output.txt` file in the current directory.

Sample data:
```
        "data": {
            "message": "I am running!"
        }
```
